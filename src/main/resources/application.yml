spring:
  datasource:
    password: Abdulmedi0906
    url: jdbc:mysql://localhost:3306/jwt_security
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true